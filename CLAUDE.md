@AGENTS.md

## UI Rules

- **Never use number input spinners/arrows** — always hide them with CSS (`appearance: textfield` + `::-webkit-inner-spin-button { display: none }`). Give users a plain text-style input for numbers.
